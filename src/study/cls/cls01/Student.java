package study.cls.cls01;

public class Student {
	// class 클래스
	// Student 학생
	
	// 클래스: 속성 + 동작 표현하는 설계도 

	// 필드(속성)
	// 학생이 가질 수 있는 속성
	String name;	
	int age;		// 인스턴스가 생성이 됐을때 필드는 heap 영역에 생성됨
	int grade;		// 필드는 heap 영역에 생성됨
	String phone; // "010 ... int는 8진수로 쓰이기 때문에 문자열로 표현해야함
	String studentID; // 학번
	
	// 생성자(초기 생성시 할일)
	
	
	//메소드(동작)
}
